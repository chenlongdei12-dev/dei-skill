# 可选脚本

脚本应通过参数或环境变量指定数据根目录，不得写死个人电脑路径。

推荐环境变量：

```bash
export NEWS_ANALYSIS_ROOT="/path/to/新闻联播分析数据"
```

现有数据目录中的抓取和月份索引脚本可以直接复用。跨电脑安装时，先安装本 Skill，再将宿主环境的抓取程序输出到 `NEWS_ANALYSIS_ROOT/新闻联播原文/YYYY/MM/`。

定时任务、模型调用和失败重试由宿主自动化系统负责，Skill只规定输入、处理方法和输出结构。
